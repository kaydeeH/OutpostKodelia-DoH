#config_version=5

assets:
    sounds:
        default:
            track: sfx
        music:
            track: music
        sfx:
            track: sfx
        voice:
            track: voice
    images:
        preload:
            load: preload